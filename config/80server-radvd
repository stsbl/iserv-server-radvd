AdverstiseNAT64:
name            Adverstise NAT64 address range on default interface
description     Select if NAT64 range should be advertised on default and internet interfaces. Normally that is not neccessary.
type            bool
default         false
readonly        true
group           Module: Radvd server

RadvdAdvertiseOnDefIf:
name            Adverstise LAN routes on default interface
description     Select if LAN routes should be advertised on default gateway interface. Normally required when obtaining prefixes dynamically using DHCPv6. Only deactivate when required by your Internet service provider.
type            bool
default         true
readonly        true
group           Module: Radvd server
