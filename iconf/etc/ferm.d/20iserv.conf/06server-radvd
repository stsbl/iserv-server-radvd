@def $UniqueLocalAddresses = `netquery6 -p -f ip | tr "\n" " "`;

domain (ip6) {
  table filter {
    chain input_first {
      proto ipv6-icmp icmpv6-type (133 134) saddr @ipfilter($UniqueLocalAddresses) jump DROP;
    }
  }
}

